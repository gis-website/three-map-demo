import { createLightCross } from '../animations/plane'

/**
 * @description: load linght cross
 * @param {any} data ploce info
 * @return {*}
 */
export const loadLightCross = (data:any):void => {
  createLightCross(data)
}
