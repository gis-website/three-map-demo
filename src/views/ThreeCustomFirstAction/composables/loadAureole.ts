import { createAureole } from '../animations/aureole'

/**
 * @description: load aureole
 * @param {any} data place info
 * @return {*}
 */
export const loadAureole = (data:any):void => {
  createAureole(data)
}
